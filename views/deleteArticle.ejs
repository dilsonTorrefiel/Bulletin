<!DOCTYPE html>
<%- include('header')%>

  <body>
    <div class="container-fluid">
      <h1 class="display-4 text-center" href="#">Articles</h1>

      <div class="container">
        <p class="lead mx-3">Name : <%= name %>
        </p>
        <!-- <h1 style="padding-left: 8px;">Name : <%= name %>
        </h1> -->
        <p class="lead mx-3">Email : <%= email %>
        </p>

        <form class="form _grades-header" action="/logout" method="POST">
          <div class="form-group  _grades-header">
            <button type="submit" class="btn btn-lg btn-info mx-3">Logout</button>
        </form>
        <p></p>
      </div>


      <div class="container">
        <% Article.forEach(function(e){ %>
          <% if(e._id===ID ){ %>
            <form action="/delete" method="post">
              <button name="deleteId" class=" btn btn-primary-outline" title="Delete student" value="<%=e._id %>"
                onCHange="this.form.submit()"><i class="fas fa-trash-alt fa-2x"></i></button>
            </form>
            <% } %>
              <% }) %>
      </div>
    </div>
  </body>

  <!-- <p id="pTag">text</p>
<script>
  document.querySelector('#pTag').innerHTML ="Dilson";
</script> -->

  <%- include('footer')%>